# frozen_string_literal: true

module RubyModKit
  # The base class of transpiler mission.
  class Mission
    def perform(Generation => _generation): void
      raise RubyModKit::Error, "Unexpected type #{self.class}"
    end
  end
end
