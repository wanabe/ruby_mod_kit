# frozen_string_literal: true

module RubyModKit
  # the base class of corrector
  class Corrector
    def correctable_error_types: (Array[Symbol])
      []
    end

    def correct(Prism::ParseError => _parse_error, Generation => _generation, Node::ProgramNode => _root_node, MemoPad => _memo_pad): void
      raise RubyModKit::Error, "Unexpected type #{self.class}"
    end
  end
end
